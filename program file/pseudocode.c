integer funn(integer a,integer b,integer c)
    for (each c from 4 to 6)
        if((5+b)+(10+c)>(a+c))
            jump out of loop
        End if
        b=a+b
        b=(3+7)+b
        End for
    return a+b